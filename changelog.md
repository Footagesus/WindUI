# 1.6.64

## Changelog:

- Added Roblox Studio Support (clone repo with `git clone` and run `rojo serve` and connect in studio)
- Added `Enum.KeyCode` support to `Keybind` element (can now accept both: string and Enum.KeyCode.\*)
- Added `ConfigManager:SetPath()` function for custom config folder path
- Added `DescFontWeight` to `Section` element (section inside the tab)
- Added `Icons.server.lua` for Studio support
- Added `Size` to `Button` element
- Added `Window.Topbar:Button(...)`
- Added `Border` to `Tab` element
- Added more Fallbacks for themes
- Added `Scale` to OpenButton
- Fixed `JunkieDevelopment` service require path
- Fixed `OpenButton` on PC Devices
- Fixed `pandadevelopment` api url
- Fixed Section height
- Fixed some bugs
- Now Dialog more rounded
- Now Themes looks good
- Moved from `Code:SetCode(code: string)` to `Code:Set(code: string)`
- Updated `main_example.lua` to use `main.client.lua`
